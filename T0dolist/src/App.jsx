import { useState } from "react";

function App () {
const [tâche, setNewTâche] seState([]);
const [newTâches, setNewTâches]=useState(");
return (
<button type = "button" onClick={handleClick}>Ajouter</button>
)
}

export default App;